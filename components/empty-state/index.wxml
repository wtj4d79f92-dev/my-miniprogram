<view class="empty">
  <text class="empty-icon">{{icon}}</text>
  <text class="empty-text">{{text}}</text>
  <view wx:if="{{buttonText}}" class="btn-primary empty-btn" bindtap="onAction">
    <text>{{buttonText}}</text>
  </view>
</view>
