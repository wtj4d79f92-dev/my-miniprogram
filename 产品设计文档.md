# 旷行吖 · 产品需求文档（PRD）

> 版本：v1.0
> 日期：2026-09-06
> 文档性质：生成微信小程序（旷行吖）功能梳理说明
> 适用对象：产品、设计、研发、测试

---

## 1. 文档说明

本文档用于完整描述「旷行吖」小程序的现状功能，作为**一比一复刻**的依据。复刻团队可按本文档完成 UI、交互、业务规则、数据结构和云函数接口的实现，最终交付一个功能等价的小程序。

复刻建议以“先 Mock 后云开发”的方式推进：

1. 先按文档实现全部页面和交互，数据层使用本地 Mock，可完整跑通浏览、筛选、发布、报名、退出、分享流程；
2. 再接入云开发，建立数据库集合、部署云函数，将数据层切换到真实服务；
3. 最后补齐主体资质、隐私协议、内容审核等上线材料。

---

## 2. 产品概述

### 2.1 产品定位

「旷行吖」是一款面向户外运动爱好者的**组队 / 摇人微信小程序**，让用户可以浏览附近的户外活动、按城市和玩法筛选、快速发起 AA 活动、一键报名组队，并通过微信群二维码与分享海报完成线下集合。

### 2.2 核心价值


| 用户角色 | 核心诉求                   | 产品提供                                       |
| -------- | -------------------------- | ---------------------------------------------- |
| 普通用户 | 找到同城、同兴趣的户外搭子 | 按城市 / 类型 / 集合日期浏览与搜索活动         |
| 组织者   | 快速发布活动、聚集人气     | 表单化发起、一键分享好友与朋友圈、生成分享海报 |
| 参与者   | 报名方便、联系可靠         | 微信一键登录、报名后展示活动群二维码           |

### 2.3 产品范围

本期功能范围（必须复刻）：

- 首页：定位城市、城市切换、横幅、户外玩法入口、热门 / 最新活动推荐、悬浮发布入口；
- 活动广场：类型筛选、城市筛选、关键词搜索、排序、集合时间（星期 / 具体日期）筛选、分页、下拉刷新；
- 活动详情：活动信息展示、报名成员列表、报名 / 退出、免责协议、群二维码弹窗、好友 / 朋友圈 / 海报分享；
- 活动发布：表单校验、封面与群二维码上传、自研日历、AA / 非 AA 费用、按类型展示难度 / 全程 / 爬升等字段；
- 个人中心：微信一键登录、头像昵称编辑、手机号可选绑定、我的参与 / 我的发布入口、发起活动入口、意见反馈、注销账号（二次确认后删除账号与其全部数据）；
- 我的活动：我参与的 / 我发布的双 Tab 列表；发起人可关闭 / 重新打开活动；
- 活动审核：用户发布 / 编辑的活动先进待审队列，机审（文本内容安全 + 封面与群二维码内容安全 + 群二维码识别）全部通过时自动放行（几秒内即在首页与广场展示），机审疑似、异常或群二维码识别不通过时才留给运营在审核台通过或驳回（可填原因）；审核动作与会话留痕，自动放行记 `auto-approve`；转人工的原因（文本检测失败、图片未出结论、图片未送检、机器需复核、二维码识别异常）在审核台直接标出，不把「没结论」显示成「机器通过」，发布当刻文本检测没拿到结论的活动会在图片结论回来时补检一次文本；
- 意见反馈：文本反馈提交；
- 后台基础能力：横幅内容管理（云数据库）、反馈数据查看（云数据库）、活动审核台（小程序内页面，仅审核人可见，权限由 admin 云函数按 openid 判定）。

---

## 3. 名词与角色


| 名词             | 说明                                                                       |
| ---------------- | -------------------------------------------------------------------------- |
| 活动类型         | 户外玩法分类：徒步、爬山、自驾游、露营、骑行、打球、健身、跑步、游泳、其他 |
| 活动标签         | 缺车主 / 缺乘客，发布时选填，仅自驾游、徒步、爬山可选                      |
| 集合时间         | 活动出发时间，仅精确到日期，用于日历选择与按星期筛选                       |
| AA 制            | 费用现场 AA 结算，不在页面填金额                                           |
| 非 AA 制         | 发布者填写固定人均费用（元）                                               |
| 发起人（主办方） | 发布活动的用户，活动仅对发起人开放“关闭 / 打开”操作                      |
| 参与者           | 报名活动的用户，数据中以 joinedPeople 记录                                 |
| Mock 模式        | 无后端模式，数据由本地生成器提供，操作写入本地缓存                         |
| 云开发模式       | 使用微信云开发数据库与云函数，真实持久化                                   |

用户角色权限：


| 能力                      | 游客               | 已登录用户 | 活动发起人 |
| ------------------------- | ------------------ | ---------- | ---------- |
| 浏览首页 / 广场 / 详情    | ✔                 | ✔         | ✔         |
| 搜索 / 筛选               | ✔                 | ✔         | ✔         |
| 发布活动                  | ✘（触发登录引导） | ✔         | ✔         |
| 报名 / 退出               | ✘（触发登录引导） | ✔         | ✔         |
| 关闭 / 打开自己发布的活动 | ✘                 | ✘         | ✔         |
| 编辑头像昵称 / 绑定手机号 | ✘                 | ✔         | ✔         |

---

## 4. 信息架构

### 4.1 页面清单


| 页面     | 路由                                                 | 入口                                                  | 是否登录后才可使用       |
| -------- | ---------------------------------------------------- | ----------------------------------------------------- | ------------------------ |
| 首页     | pages/home/home                                      | 底部导航第 1 个 Tab                                   | 否                       |
| 活动广场 | pages/square/index                                   | 底部导航第 2 个 Tab、首页搜索框 / 玩法入口           | 否                       |
| 个人中心 | pages/usercenter/index                               | 底部导航第 3 个 Tab                                   | 否（未登录展示登录引导） |
| 发布活动 | pages/activity/publish/index                         | 首页 / 广场悬浮发布按钮、个人中心“发起新活动”       | 是                       |
| 活动详情 | pages/activity/detail/index?id=xx                    | 首页、广场、我的活动中的活动卡片；分享链接            | 否                       |
| 我的活动 | pages/user/activity-list/index?tab=joined\|published | 个人中心统计区、“我的参与”、“我的发布”            | 是                       |
| 意见反馈 | pages/feedback/index                                 | 个人中心“意见反馈”                                  | 否                       |

### 4.2 页面关系

```text
底部 Tab
├── 首页
│   ├── 活动广场（Tab 切换）
│   └── 发布活动 → 活动详情
├── 活动广场
│   ├── 发布活动 → 活动详情
│   └── 活动详情
└── 个人中心
    ├── 我的活动（joined / published）→ 活动详情
    ├── 发布活动 → 活动详情
    └── 意见反馈

活动详情
├── 微信好友 / 朋友圈分享（打开详情页）
└── 生成海报 → 保存到相册 / 长按转发
```

### 4.3 底部导航

自定义 Tab Bar，共 3 项：首页、广场、我的。选中态高亮主品牌色，切换时高亮索引与当前路由同步；在非 Tab 页面不展示。

---

## 5. 全局设计规范

### 5.1 品牌与视觉


| 元素          | 规范                                                                        |
| ------------- | --------------------------------------------------------------------------- |
| 产品名        | 旷行吖                                                                      |
| 产品口号      | 和志同道合的人一起出发                                                      |
| 主色 / 品牌绿 | `#00B578`                                                                   |
| 辅助色        | 青蓝`#45B7D1`、薄荷绿 `#4ECDC4`、活力橙 `#FF7D00` 等                        |
| 页面背景      | 浅色渐变（白到淡蓝绿），正文深灰`#2D3748`                                   |
| 默认封面海报  | 类型渐变底 + “旷行吖”标题 + “和志同道合的人一起出发”副标题 |
| 卡片风格      | 圆角、白底、轻微阴影；关键展示项用“字段标签 + 值”结构                     |
| 单位规范      | 距离 km、爬升 m、难度 X星、费用 ¥ / AA制 / 免费                            |
| 加载/空态     | 统一使用 TDesign 加载、空状态组件；接口加载有 loading 反馈                  |

### 5.2 通用交互规则

- 页面首次加载必须有加载态，失败时静默降级为“已加载空数据”，不阻塞页面浏览；
- 列表页支持下拉刷新；加载更多由“点击底部加载更多”+ 触底自动加载双入口触发；
- 需要登录的操作统一走“登录守卫”：直接打开全屏登录页（页面内展示触发登录的原因），登录成功后继续原操作；点“暂不登录”或左上角返回箭头则中断原操作；
- 首页 / 广场 / 我的三个主页面 `onShow` 时重新拉取数据，保证新发布或报名后返回即刷新；
- 页面内常用提示采用 Toast / 弹窗，不使用原生 alert；
- 表单必填项以“*”标识，校验失败以 Toast 提示具体原因；
- 悬浮发布按钮固定于首页与广场右下角，绿色圆钮 + “发布”文字；
- 切换类型带参跳 Tab 页时，通过本地缓存 `square_pending_type` 传递（Tab 页无法通过 URL 传参），目标页消费后删除。

### 5.3 全局状态与本地存储


| 存储键                | 用途                     | 写入时机            | 消费 / 清除      |
| --------------------- | ------------------------ | ------------------- | ---------------- |
| `aa_selected_city`    | 用户手动选择的城市       | 切换城市            | 重新定位时清除   |
| `square_pending_type` | 跳转广场后预选的活动类型 | 首页玩法 / 横幅点击 | 广场页读取后清除 |
| `my_user`             | Mock 登录用户信息        | 登录 / 注册         | —               |
| `my_user_counter`     | Mock 注册序号            | 首次注册            | —               |
| `my_last_phone`       | 上次登录手机号           | 登录成功            | —               |
| `my_published`        | Mock 已发布活动          | 发布成功            | —               |
| `my_joined`           | Mock 已报名活动 ID 列表  | 报名成功            | —               |
| `my_feedback`         | Mock 反馈记录            | 提交反馈            | —               |

---

## 6. 功能需求详述

### 6.1 登录与账号（F1）

#### 6.1.1 需求说明

使用微信 openid 实现“一键登录”，登录不需要强制手机号。登录成功后可引导完善微信头像与昵称。

#### 6.1.2 功能规则

**一键登录（云开发模式）**

1. 未登录用户触发登录：底部出现 loading“登录中”，调用云函数完成 openid 注册 / 登录；
2. 首次注册：系统按注册先后生成递增用户编号 `userId`（第 1 个注册用户为 1），昵称默认“微信用户”；
3. 再次登录：保留已设置的昵称 / 头像，仅补充手机号与资料字段；
4. 用户登录后，默认头像为灰色默认图，昵称显示为“微信用户”。
5. 在个人中心顶部，用显眼但非阻断式的卡片或入口提示：“完善头像昵称，让好友认出你 >”。
6. 用户点击这个入口，才弹出编辑框进行修改，在编辑弹窗里增加一个 **“×”关闭按钮** 或 **“暂不完善”** 的文字按钮，用户点关闭后，回到个人中心，头像/昵称仍然保持默认状态。
7. 登录失败 Toast“登录失败，请重试”。

**一键登录（Mock / 演示模式）**

- 全屏登录页结构（信息结构参考「神州租车」登录页）：顶部返回箭头 + 标题，品牌区，提示“未注册手机号验证登录后自动注册旷行吖账号”，协议勾选行，主按钮“登录此账号 / 手机号快捷登录”，次按钮“手机号快捷登录”，“切换手机号”，底部“暂不登录”；
- 协议默认不勾选，勾选后才允许登录；未勾选时点击任一登录入口，Toast 提示“请先阅读并同意协议”并抖动协议行；协议文字可点击查看《用户服务协议》《隐私政策》正文；
- 已登录过手机号时，主按钮展示“登录此账号”+ 脱敏手机号，一键复用该账号；没有历史手机号时进入手机号输入页；
- 手机号输入页：`+86` 前缀 + 11 位输入框；格式必须为 1 开头的 11 位数字，否则提示“请输入正确的11位手机号”；
- 点击底部“暂不登录”或左上角返回箭头：取消登录并中断原操作；
- 首次登录昵称自动生成为脱敏手机号（前 3 位 + **** + 后 4 位），再次登录保留原资料。

**头像 / 昵称编辑（登录后）**

- 头像使用微信官方 `chooseAvatar` 能力选择；
- 昵称输入框使用微信官方 nickname 输入能力，最长 20 字；
- 昵称必填，为空提示“请填写昵称”；
- 保存成功后，用户已发布活动的“发起人头像昵称”需同步更新。

**手机号绑定（可选）**

- 已登录但未绑定手机号时，个人中心顶部展示“绑定手机号（可选）”入口；

* **绑定手机号按钮的上方/附近**：加上“我已阅读并同意《用户服务协议》和《隐私政策》”的**复选框（checkbox）**，默认不勾选。
* **协议文本可点击查看**：将《用户服务协议》和《隐私政策》做成可以点击的链接，点击后能查看具体内容。
* **按钮联动逻辑**：
  * 当**复选框未勾选**时，“绑定手机号”的按钮不可点击，或者点击后弹窗提示“请先阅读并同意协议”。
  * 只有当用户**主动勾选**复选框后，“绑定手机号”按钮才变为可点，然后才能让用户手动输入。

- 通过微信手机号授权获取手机号，绑定成功后 Toast“绑定成功”；
- 用户拒绝手机号授权不阻断使用，Toast 提示“手机号授权不可用，不影响登录使用”；
- Mock 模式手机号在登录页中一并输入，无需单独绑定。

#### 6.1.3 异常处理

- 未授权手机号 / 授权失败：不阻塞登录与浏览；
- 保存资料失败：保持弹窗打开并提示“保存失败，请重试”；
- 未登录点击个人资料区：展示登录引导，不弹出编辑框。

---

### 6.2 城市定位与切换（F2，全局能力）

#### 6.2.1 需求说明

首页与广场顶部提供“当前城市 + 切换城市 + 重新定位”入口，数据按城市过滤；未开启定位时允许用户手动选择城市或直接浏览全国。

#### 6.2.2 功能规则

**自动定位**

1. 首次进入且用户未手动选过城市：调用微信定位接口（gcj02）；
2. 定位成功后优先通过腾讯位置服务逆地址解析获取城市名（需配置地图 key）；未配置 / 失败时按经纬度就近匹配内置城市坐标表；
3. 匹配失败显示“全部”；
4. 未授权定位：状态记录为“定位未开启”，不重复弹授权；页面仍可浏览并手动切换城市；
5. 已授权的定位结果在本会话内缓存，避免反复弹窗。

**手动切换城市**

- 省级 / 市级双列滚动选择器（“全部”省与“全部”市置于最前）；
- 选择省后城市列联动重置为第一个（全部）；
- 选择「省 + 全部」按全省筛选（如“广东省 + 全部”= 广东范围内的活动）；
- 省与市都选“全部”即浏览全国活动；
- 手动选择结果全局同步并持久化，下次冷启动沿用；重新定位时清除。

**城市如何与活动关联**

- 城市归属一律由服务端算：发布 / 编辑时按 `locationAddress`（地图选点反查的完整地址，不展示）优先、其次 `location` 文本匹配出 `city` 落库；地址里认不出城市（手输「双流区润和路附近」这类缺省市的短地址）时，用发布页随表单上送的「发布者当前城市」兜底，该值只认唯一城市（省份 / 全国一律不采用），其他情况前端传值不作数；发布页在集合地点下方同步提示归属结果，避免发布后「按城市筛选找不到自己的活动」；
- 存量数据：本次改动之前发布的、`city` 为空的活动不会被任何城市筛选命中，发起人在「我的活动」里编辑后重新提交即可按上面的规则重新归属；
- 首页 / 广场按 `city` 精确筛选，关键词检索同时命中 `title`、`location` 与 `locationAddress`（模糊包含）；
- Mock 模式：按地点匹配出的 `city` 精确过滤；
- 若按当前城市无结果，先 Toast“暂无符合要求的活动”，再自动回退展示全国推荐，并展示提示条“暂无符合要求的活动，为你推荐以下活动”（首页为“为你推荐其他活动”）。

---

### 6.3 首页（F3）

#### 6.3.1 需求说明

首页是“浏览与转化”主入口，自上而下依次为：定位栏、搜索入口、横幅轮播、户外玩法入口、热门组队、最新发布、页脚文案与悬浮发布按钮。

#### 6.3.2 区块与交互


| 区块       | 内容与交互                                                                                                                             |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| 顶部定位栏 | 左侧定位图标 + 城市名（未授权显示“定位未开启”）；右侧“切换城市”打开省市选择器；点击左侧执行重新定位（未授权先引导开启设置）        |
| 搜索入口   | 仿搜索框文案“搜活动、地点，加入旷行吖”+“搜索”按钮，点击跳转活动广场 Tab                                                          |
| 横幅轮播   | 自动轮播（4 秒间隔）、循环、带指示点；每条含主标题 / 副标题 / emoji / 渐变背景；点击执行动作：跳广场并按“当前定位城市 + 横幅对应活动类型”筛选（定位未就绪时先定位再跳），或进入发布页（需登录） |
| 户外玩法   | 标题“发现户外玩法”；9 个玩法 + “其他”图标网格；点击跳广场并在“活动类型”筛选中预选该类型；图标加载失败时回退显示 emoji                   |
| 热门组队   | 标题“热门组队”+“更多”入口；按参与人数倒序展示最多 6 个活动卡片                                                                     |
| 最新发布   | 标题“最新发布”；按发布时间倒序展示最多 3 个活动卡片；无数据不展示该区块                                                              |
| 页脚       | 文案“— 和志同道合的人一起出发 —”                                                                                                   |
| 悬浮发布   | 右下角“+”发布按钮，需登录后进入发布页                                                                                                |

#### 6.3.3 数据规则

- 接口按当前城市返回横幅、玩法类型、热门 6 条、最新 3 条；
- 玩法类型与样式由类型字典下发（云模式首页接口返回；Mock 直接取本地字典）；
- 卡片图片若为云存储文件 ID，先换取临时链接再渲染。

#### 6.3.4 空态与异常

- 当前城市无活动：Toast + 全国回退推荐；
- 接口失败：结束 loading，页面正常渲染其他可展示内容，不阻塞。

---

### 6.4 活动广场（F4）

#### 6.4.1 需求说明

活动广场是核心列表页，支持城市、类型、集合时间（星期 / 具体日期）、排序、关键词搜索多条件组合查询，并带分页。

#### 6.4.2 界面结构

自上而下：

1. 定位 / 城市栏（同首页，行为一致）；
2. 搜索栏：占位“搜索活动、地点”，支持回车提交与一键清空；
3. 排序栏：最新发布 / 最热门二选一（默认“最新发布”），右侧为三组筛选胶囊——活动类型下拉（全部类型 + 10 个玩法，未选时显示“全部类型”，选中后显示类型名并高亮）、日期选择器（未选时显示“选择日期”，选中后显示 `09-18` 并带清除按钮）与星期下拉（不限周几、周一至周日），选中后展示当前条件；
4. 活动卡片列表；
5. 底部加载提示：“加载更多” / “已经到底啦”；
6. 悬浮发布按钮。

#### 6.4.3 筛选与排序规则


| 条件     | 规则                                                                          |
| -------- | ----------------------------------------------------------------------------- |
| 城市     | 同 6.2；「省 + 全部」按全省筛选，省份与城市都选“全部”时不做该维度过滤         |
| 类型     | 单选（活动类型筛选胶囊，全部类型 / 徒步 / 爬山 / 自驾游 / 露营 / 骑行 / 打球 / 健身 / 跑步 / 游泳 / 其他），切换后回到第 1 页刷新；首页“发现户外玩法”点击某类型跳转进来时预选该类型 |
| 关键词   | 匹配活动标题或集合地点，忽略大小写；输入变化只保存，提交 / 清空时刷新         |
| 集合时间 | 按活动开始日期过滤：星期几（不限周几 / 周一至周日）与具体日期二选一。选具体日期走自研日历，最早可选今天，选中后收起星期条件；选星期几会清掉具体日期，两者互斥避免叠加后查不出结果 |
| 排序     | 最新发布（默认）=发布时间倒序；最热门=已报名人数倒序                           |
| 已关闭活动 | 只在广场保留「关闭当天」这一个自然日，次日起不再展示；在列表里沉底，无论按哪个维度排序都排在未关闭活动之后。首页热门 / 最新不展示已关闭的活动 |
| 分页     | 每页 10 条；下拉刷新重置到第 1 页；触底且未满时自动加载，也可点击“加载更多” |

#### 6.4.4 空态与回退

- 带条件（城市 / 类型 / 星期 / 具体日期 / 关键词）查询无结果：Toast“暂无符合要求的活动”，随后自动清空城市与类型条件，保留排序 / 关键词 / 星期 / 具体日期，回退展示推荐活动，列表上方提示“暂无符合要求的活动，为你推荐以下活动”；
- 完全无数据：空态图“没有找到相关活动，发布一个吧”，提供“去发布”按钮（需登录）；
- 接口异常：停止 loading，可下拉重试。

---

### 6.5 活动详情（F5）

#### 6.5.1 需求说明

详情页聚合活动全部信息与核心转化按钮（报名 / 退出 / 分享），并承担协议确认、群二维码展示、海报生成等关键能力。

#### 6.5.2 页面结构

**顶部封面区**

- 封面图（可缺省）：有封面展示图片；无封面使用默认“旷行吖”海报底图；
- 右上覆盖类型胶囊标签；
- 封面底色来自活动类型渐变。

**活动主体区**

1. 标题行：可选难度标签（X星）、活动标签（缺车主 / 缺乘客）、活动标题；
2. 发起人行：头像（有图展示图 / 无图展示首字彩色块）、昵称、固定标签“发起人”、右侧集合日期；
3. 地点行：字段“地点”+ 集合地点文本 + “导航”胶囊；点击整行调起微信内置地图，用户在地图页点“导航”并选择地图软件（高德 / 百度 / 腾讯 / 苹果地图）后，坐标与地址一起带过去直接开始导航。地图选点发布的活动用落库坐标直接打开（无需解析，地点文本带不带省市都不影响）；只有地址文本的活动先做地理编码（活动城市 / 地址文本 / 当前定位城市依次作为参考区域，缺省市的短地址会补上城市再查一次，地图选点的地点文本本身就带「省 + 市」，命中率更高，前提是配置了腾讯位置服务 key 并把 `apis.map.qq.com` 加进 request 合法域名），解析不出坐标时不再只弹「无法自动定位」的死胡同，而是给出「在地图上点选位置后导航」（地图页预填地址、从城市中心打开，选中的坐标直接开导航）与「仅复制地址」两条路；
4. 徒步 / 爬山专属指标行：字段“全程”+“爬升”，未填写（值为 0 / 空）时该行不展示；
5. 信息卡：
   - 集合时间（日期 + 星期）；
   - 返程时间（“待定”占位兜底）；
   - 费用：AA制 / ¥金额 / 免费；
   - 人数：已报名人数 / 上限 人；
   - 状态：招募中（绿）/ 已满（灰描边）/ 已关闭（灰描边）；
6. 已报名卡：标题“已报名 (N)”，成员头像 + 昵称平铺；无人时文案“还没有人报名，快来抢沙发”；
7. 活动介绍卡：展示介绍全文（保留换行）。

**底部操作区**

- 未报名时展示协议勾选行：“我已阅读并同意《活动风险告知与免责协议》”，可点击文本查看协议半屏弹窗；
- 主按钮四态：
  - 可报名且未满：绿色“我要报名”；
  - 已满且未报名：禁用“已满员”；
  - 已关闭：禁用“已关闭”；
  - 已报名：描边“退出活动”；
- 发起人查看自己发布的活动：不展示报名协议行，主按钮替换为活动管理按钮——招募中为描边“关闭活动”，已关闭为主色“重新打开活动”，点击后弹确认框调用 `toggle` 接口，状态与按钮文案即时刷新；
- 左侧“分享”按钮打开分享面板；已报名用户（非发起人）在“退出活动”旁多一个“查看二维码”按钮，与退出活动同尺寸同字体、主色实底反白字，点击直接重新弹出活动群二维码弹窗（文案为“扫码加入活动群”，不再提示“报名成功”），发起人未上传群二维码时不展示该按钮。

**活动不存在**

- 显示“活动不存在或已下架”+ 返回按钮（优先返回上一页，无上一页则回广场 Tab）。

#### 6.5.3 报名 / 退出流程

**报名**

1. 点击“我要报名”→ 登录守卫；
2. 未勾选免责协议 → 弹窗“请先阅读并同意免责协议”，文案提示“报名前需勾选《活动风险告知与免责协议》。阅读并同意后将直接完成报名，无需再次确认。”，按钮“去阅读”/“稍后再说”；选“去阅读”打开协议半屏弹窗，点击“同意并继续”后置为已勾选并直接继续报名；关闭协议弹窗则不再自动延续报名；
3. 已勾选免责协议即视为完成报名确认，直接调用报名接口，不再弹“确认报名参加本次活动？”二次确认；
4. 成功且活动有群二维码 → 弹出“报名成功，扫码加入活动群”，大图二维码可长按识别，底部“完成”关闭；无群二维码则 Toast“报名成功”；
5. 报名成功后按钮态切换为“退出活动”，人数与成员列表即时更新；
6. 接口返回活动已关闭 → Toast“活动已关闭，无法报名”；
7. 满员时前端禁用，后端同样拒绝追加。

**退出**

1. 已报名用户点击“退出活动”→ 确认弹窗“确定要退出本次活动吗？”；
2. 确认后调用退出接口，Toast“已退出”，协议勾选恢复未勾选，人数与成员列表刷新。

#### 6.5.4 协议内容与交互

- 协议全文见附录 12.3；
- 半屏弹窗：标题 + 可滚动正文（加粗段落重点高亮）+“暂不同意”/“同意并继续”按钮；
- “同意并继续”置为已勾选并关闭弹窗；若该弹窗由报名提醒进入，则同意后直接继续完成报名。

---

### 6.6 活动分享与海报（F6）

#### 6.6.1 分享面板

详情页点击“分享”弹出底部面板，含三项：


| 渠道         | 行为                                                                       |
| ------------ | -------------------------------------------------------------------------- |
| 微信好友     | 调起系统分享，卡片标题为“{活动标题}，一起来组队吧！”，落地页为该活动详情 |
| 朋友圈       | 提示用户点右上角“···”→“分享到朋友圈”，分享内容同好友               |
| 生成分享图片 | 生成 750 × 1280 分享海报并预览                                            |

页内同时监听右上角“转发给朋友 / 分享到朋友圈”菜单。

#### 6.6.2 海报规格

画布 750 × 1280 竖版，内容自上而下：

1. 浅绿到浅蓝白渐变背景；
2. 左上角白色圆角框内为小程序码（云端按活动生成并缓存；缺失时绘制“小程序码”占位）；
3. 右上角品牌名“旷行吖”+ 口号“和志同道合的人一起出发”；
4. 中部标题：活动标题最多两行，超长省略；
5. “活动二维码”说明 + 白色圆角卡片内展示活动群二维码（无图时占位文案“活动群二维码 / 发布活动时可上传”），下方小字“扫码报名 加入活动”；
6. 底部白卡“活动要求”：集合时间、集合地点、费用与人数、活动介绍（最多 2 行）。

海报预览页提供“长按转发 / 保存”提示、“保存图片”按钮（调相册保存能力）与“关闭”。未授权相册时引导打开设置。

---

### 6.7 发布活动（F7）

#### 6.7.1 需求说明

以表单 + 分段面板方式发起活动。所有字段位于同一页面，提交前统一校验，成功后自动进入活动详情页。

#### 6.7.2 表单字段


| 字段         | 组件              | 必填            | 默认值       | 限制 / 规则                                                   |
| ------------ | ----------------- | --------------- | ------------ | ------------------------------------------------------------- |
| 封面         | 图片上传          | 否              | 默认旷行吖海报 | 相册 / 拍照，压缩图，单张；缺省使用默认海报                   |
| 活动类型     | 类型网格          | 是              | 徒步         | 单选；切换时清理不适用字段（见 6.7.3）                        |
| 活动标签     | 标签双选          | 否              | 无           | 仅自驾游 / 徒步 / 爬山出现；缺车主 / 缺乘客，可多选           |
| 活动标题     | 输入框            | 是              | 空           | 最长 30 字，占位“一句话说清活动内容”                        |
| 集合时间     | 自研日历          | 是              | 明天         | 日期面板选择，详见 6.7.4                                      |
| 返程时间     | 自研日历          | 是              | 后天         | 不得早于集合时间                                              |
| 集合地点     | 输入框 + 地图选点 | 是              | 空           | 最长 50 字；地图选点后输入框显示「选点地址的省 + 市 + 地点名」（如“四川省成都市 华府大道地铁站”；地点名本身已带省市或退回完整地址时不重复加），其对应的完整地址另存 `locationAddress`（最长 100 字，不出现在任何展示位，只供城市匹配与地址检索）、坐标另存 `locationLat` / `locationLng`（同样不展示，供详情页与卡片点地址调起导航）；地址与坐标随表单一起上送，不要求用户看见；改成手输且与上次选点无关时地址与坐标一起清空（只是删掉自动补上的省市前缀仍算同一个地点，地址与坐标保留） |
| 活动难度     | 选择器            | 徒步 / 爬山必填 | 3星          | 1–10 星                                                      |
| 全程长度     | 数字输入          | 否（徒步 / 爬山可填） | 空      | 选填；单位 km，仅允许数字与小数点，填写时须 > 0               |
| 累计爬升     | 数字输入          | 否（徒步 / 爬山可填） | 空      | 选填；单位 m，填写时须 > 0                                    |
| 费用模式     | 双态切换          | 是              | AA制         | AA制 / 非AA制                                                 |
| 人均费用     | 数字输入          | 非AA制必填      | 0            | > 0，单位元                                                   |
| 人数上限     | 步进器            | 是              | 10           | 2–100，整数                                                  |
| 活动群二维码 | 图片上传          | 是              | 空           | 仅相册，单张；须能识别出微信群邀请链接；可重新上传 / 删除      |
| 活动介绍     | 多行文本          | 否              | 空           | 最长 500 字                                                   |
| 发布协议     | 勾选              | 是              | 否           | 同意《活动发布免责协议》后可提交                              |

#### 6.7.3 类型联动规则

- 选择徒步或爬山：显示难度、全程、爬升、活动标签（与自驾游同规则）；切换为其他类型时清空距离 / 爬升；
- 选择自驾游、徒步、爬山：显示“缺车主 / 缺乘客”标签；切到其他类型清除已选标签；
- 其余类型（打球、骑行、健身、游泳、露营、跑步、其他）：隐藏标签、难度、全程、爬升字段。

#### 6.7.4 自研日历

- 弹层式月历，标题“选择日期”，含年 / 月、上一月 / 下一月按钮、星期表头（日—六）、42 格日期网格；
- 相邻月日期置灰展示；今天与选中日期有独立高亮样式；
- 点击日期为“暂选”，点“确定”后写回当前编辑的时间字段（集合 / 返程共用）；点“取消”丢弃；
- 支持前后翻月，不限制最早日期（复刻时建议补充禁止早于今天的规则，当前版本未实现）。

#### 6.7.5 提交校验顺序

1. 是否已勾选《活动发布免责协议》，未勾选弹提示；
2. 标题非空；
3. 集合时间已选；
4. 返程时间已选；
5. 返程不早于集合时间；
6. 集合地点非空；
7. 徒步 / 爬山：全程长度、累计爬升为选填，留空跳过；填写了则须 > 0；
8. 非 AA 制：人均费用非空且 > 0；
9. 活动群二维码已上传。

校验通过后按钮进入“发布中”loading，提交成功 Toast“发布成功”并延时跳转新活动详情页。

#### 6.7.6 发布成功的数据落库

- 服务端补全类型外观（emoji / color / bg）、城市（地点自动匹配）、星期、空成员列表、创建时间、招募中状态、发起人快照（openid + 昵称 + 头像）；
- 封面与群二维码为本地临时路径时，云模式先上传到云存储再写入；Mock 模式直接保存临时路径（当前会话有效，属已知限制）。

---

### 6.8 我的活动（F8）

#### 6.8.1 需求说明

管理用户参与与发布的活动。

#### 6.8.2 页面规则

- 顶部双 Tab：我参与的 / 我发布的，均分宽度，切换即重新拉取；
- 我参与的：活动列表按开始时间升序；
- 我发布的：活动列表按发布时间倒序，每张卡片底部追加工具栏：
  - 左侧活动状态：“招募中” / “已关闭”；
  - 右侧操作按钮：“关闭” / “打开”；
- 关闭 / 打开交互：弹窗确认后调用接口；
  - 关闭文案：“关闭后，活动仅在广场展示当天，次日起不再展示，其他用户将无法报名。确定关闭吗？”
  - 打开文案：“确定重新打开该活动，恢复报名吗？”
  - 成功 Toast“已关闭 / 已打开”，单条卡片状态即时更新；
  - 接口仅发起人可操作，非发起人调用返回禁止操作。
- 下拉刷新；
- 空态：
  - 我参与的：“还没有参与活动”+“去逛逛”按钮（回广场）；
  - 我发布的：“还没有发布过活动”+“去发布”按钮。
- 点击卡片进入活动详情。

---

### 6.9 个人中心（F9）

#### 6.9.1 需求说明

集中展示用户身份、数据统计与功能入口。

#### 6.9.2 页面结构

**头部用户卡片**

- 未登录：默认头像 +“未登录”+ 副文案“登录后可使用报名、发布等功能”+“微信一键登录”按钮；
- 已登录：头像（图或首字色块）、昵称、`ID：{userId}`、个性签名（缺省“和有趣的人一起出发”）、右箭头；点击整卡打开资料编辑弹窗；
- 点击登录按钮（Mock 模式）弹出手机号输入模拟登录。

**统计区**

- 我参与的（N）：点击进入我的活动 joined Tab；
- 我发布的（N）：点击进入我的活动 published Tab；
- 中间分隔线，页面展示前加载统计。

**功能入口区**


| 入口               | 跳转 / 行为                                                                  |
| ------------------ | ---------------------------------------------------------------------------- |
| 绑定手机号（可选） | 已登录且无手机号时展示；微信手机号授权绑定；副文案“用于找回账号与活动联系” |
| 我的参与           | 我的活动 joined                                                              |
| 我的发布           | 我的活动 published                                                           |
| 发起新活动         | 发布页（登录守卫）                                                           |
| 逛逛活动广场       | 广场 Tab                                                                     |
| 意见反馈           | 反馈页                                                                       |
| 关于旷行吖         | 弹窗产品介绍                                                                 |
| 注销账号           | 已登录时展示；两次确认后删除账号、报名记录与发布的活动（不可恢复，同一微信再登录为新账号） |
| 版本               | 页脚“旷行吖 v1.0.0”                                                        |

#### 6.9.3 资料编辑弹窗

- 标题“完善头像昵称”，副文案“设置后将展示在你的活动卡片中”；
- 头像按钮调微信头像选择能力，昵称输入使用微信昵称输入能力，最长 20 字；
- 保存中防重复提交；成功后刷新头部并同步已发布活动发起人信息。

---

### 6.10 意见反馈（F10）

#### 6.10.1 功能规则

- 文本域占位“请描述你的意见、建议或遇到的问题”，最长 500 字，字数实时计数（x/500）；
- 空内容提示“请先填写反馈内容”；少于 5 字提示“反馈内容至少 5 个字”；
- 提交按钮提交中置灰，成功后 Toast“提交成功，感谢反馈”并延时返回上一页；
- 云模式写入 feedback 集合并附带 openid、昵称、头像、时间与待处理状态；
- 页脚说明“你的反馈将仅用于产品改进分析，感谢支持”。

---

### 6.11 活动卡片组件（F11，复用组件）

列表复用统一卡片：

- 上半部封面：封面图或默认海报、左上类型名、右上“已关闭”蒙层（仅关闭态）；
- 标题行：难度 / 标签前置，标题单行省略；
- 发起人行：小头像 + 昵称、右侧集合日期（格式：08月16日 周六）；
- 地点行：“地点”标签 + 地点文本单行省略 + “导航”胶囊；点这一行（`catchtap`，不会顺带进详情）直接调起地图导航，行为与详情页地点行一致；
- 徒步 / 爬山指标行：全程、爬升（留空则不展示）；
- 底部：成员头像堆叠 +“x/y人”、右侧费用（AA制 / ¥xx / 免费）；
- 支持自定义底部插槽（我的活动-我发布的工具栏使用）；
- 点击整卡触发跳详情事件。

---

## 7. 业务规则与状态机

### 7.1 活动状态


| 状态   | 编码       | 用户可见 | 广场可见                              | 可报名 | 发起人可操作 |
| ------ | ---------- | -------- | ------------------------------------- | ------ | ------------ |
| 招募中 | recruiting | 招募中   | 是                                    | 是     | 关闭         |
| 已关闭 | closed     | 已关闭   | 关闭当天可见（封面带已关闭蒙层），次日起不再展示 | 否     | 打开         |

关闭活动只在广场保留关闭当天这一个自然日（关闭时间 `closeTime` 落在今天），次日起从广场消失；首页热门 / 最新不展示已关闭的活动，详情与「我的活动」不受影响，仅禁止新报名，已报名成员不受影响。

### 7.2 报名与容量

- 每人每活动最多报名 1 次，重复报名接口幂等返回当前活动；
- 已报名人数达到人数上限即满员；
- 满员后按钮禁用“已满员”；退出腾出名额后可再次报名；
- 发起人不默认占位，可自行报名；
- 成员列表保存用户快照（openid、昵称、头像色、首字），退出后从列表移除，不保留历史记录。

### 7.3 类型字段规则速查


| 类型       | 标签 | 难度 | 全程 / 爬升 |
| ---------- | ---- | ---- | ----------- |
| 徒步、爬山 | ✔   | ✔   | ✔          |
| 自驾游     | ✔   | ✘   | ✘          |
| 其余玩法   | ✘   | ✘   | ✘          |

### 7.4 其他边界规则

- 城市为空时活动属于“全国”，全国 / 全部筛选可见；
- 关键词同时支持标题与地点；
- 发布时仅存日期，无时分概念；展示统一用“日期 + 星期”；
- 分享朋友圈需要用户手动通过右上角菜单完成，小程序内按钮仅作引导。

---

## 8. 数据需求

### 8.1 概览

云开发模式使用 4 个集合：activities、users、banners、feedback。Mock 模式数据由本地模型生成（与下方结构保持一致，增加 id 字段）。

### 8.2 users 集合


| 字段        | 类型   | 说明                             |
| ----------- | ------ | -------------------------------- |
| _id         | string | 系统文档 ID                      |
| openid      | string | 微信身份标识，唯一               |
| userId      | number | 注册序号，从 1 递增              |
| nickName    | string | 昵称                             |
| avatarUrl   | string | 头像（云文件 ID 或临时链接）     |
| avatarColor | string | 无头像时色块背景色               |
| avatarText  | string | 无头像时首字                     |
| phone       | string | 可选绑定手机号                   |
| bio         | string | 个性签名                         |
| hobbyTypes  | array  | 偏好类型（当前产品未使用，保留） |
| createTime  | number | 注册时间戳                       |

### 8.3 activities 集合


| 字段               | 类型          | 说明                                        |
| ------------------ | ------------- | ------------------------------------------- |
| _id                | string        | 系统文档 ID，对外统一补 id                  |
| type               | string        | 类型 key                                    |
| typeName           | string        | 类型中文名                                  |
| emoji / color / bg | string        | 类型展示素材（emoji、主题色、渐变背景）     |
| title              | string        | 活动标题                                    |
| cover              | string        | 封面（空串使用默认海报）                    |
| groupQrCode        | string        | 活动群二维码（必传）                        |
| desc               | string        | 活动介绍                                    |
| location           | string        | 集合地点（展示值：地图选点为「省 + 市 + 地点名」，手输为用户输入） |
| locationAddress    | string        | 地图选点反查出的完整地址，不展示，仅用于城市匹配与地址检索 |
| locationLat        | number        | 地图选点经纬度（纬度），不展示，仅用于点击地址调起导航；手输地址或无选点的历史活动为 0 |
| locationLng        | number        | 地图选点经纬度（经度），规则同 locationLat               |
| city               | string        | 服务端按 locationAddress 优先、其次 location 匹配出的城市；都认不出时用发布页上送的当前城市兜底（只认唯一城市，空为全国 / 未归属） |
| startTime          | number        | 集合时间时间戳                              |
| endTime            | number        | 返程时间时间戳                              |
| startWeekday       | number        | 集合日期星期（0=周日）                      |
| difficulty         | number        | 难度 1–10，非徒步 / 爬山为 0               |
| distance           | number        | 全程 km                                     |
| elevationGain      | number        | 累计爬升 m                                  |
| fee                | number        | 费用金额，AA 制为 0                         |
| feeMode            | string        | aa / fixed                                  |
| maxPeople          | number        | 人数上限 2–100                             |
| tags               | array         | needOwner / needPassenger                   |
| joinedPeople       | array<Member> | 已报名成员快照                              |
| joinedCount        | number        | 冗余报名数，排序用                          |
| organizer          | object        | 发起人快照                                  |
| createTime         | number        | 发布时间戳                                  |
| status             | string        | recruiting / closed                         |
| closeTime          | number        | 关闭时间戳（未关闭为 0）：广场只保留关闭当天，重新打开时归零 |
| miniQrCode         | string        | 活动详情页小程序码云文件 ID（懒生成并缓存） |

Member 结构：`{ openid, nickName, avatarColor, avatarUrl, avatarText }`。

### 8.4 banners 集合


| 字段             | 类型   | 说明                                         |
| ---------------- | ------ | -------------------------------------------- |
| _id              | string | 文档 ID                                      |
| title / subtitle | string | 主 / 副标题                                  |
| emoji            | string | 装饰 emoji                                   |
| bg               | string | 渐变背景                                     |
| action           | object | { type: square / publish, value?: 类型 key } |
| sort             | number | 排序值，升序                                 |

首页接口发现集合为空时自动写入默认 3 条（默认横幅文案见附录 12.2）。
`action.type = square` 时，客户端跳广场并预选 `action.value` 对应的活动类型，同时带上用户当前定位城市一起筛选；`action.type = publish` 时进入发布页（需登录）。
历史版本中默认横幅 3（想去看海呀）配的是 `publish`，客户端 `utils/dict.js → normalizeBanners` 与云函数 `upgradeDefaultBannerActions` 会各自做一次幂等升级：只修正“固定 _id + 默认标题 + 历史动作”三条都对得上的文档，后台自定义过的横幅不受影响。

### 8.5 feedback 集合


| 字段                 | 类型   | 说明                     |
| -------------------- | ------ | ------------------------ |
| _id                  | string | 文档 ID                  |
| openid               | string | 提交用户                 |
| content              | string | 反馈内容（截断 1000 字） |
| nickName / avatarUrl | string | 用户快照                 |
| createTime           | number | 提交时间                 |
| status               | string | 默认 pending             |

### 8.6 云函数接口（action 路由）

统一云函数名 activity，返回结构失败为 `{ code, message }`，成功为业务数据：


| action     | 入参                                                        | 返回                                     |
| ---------- | ----------------------------------------------------------- | ---------------------------------------- |
| home       | { city }                                                    | { banners, hotList, newestList }         |
| list       | { type, city, keyword, pageIndex, pageSize, sort, weekday, date } | { list, hasMore }                        |
| detail     | id                                                          | 活动（附当前用户 joined 标记）或 null    |
| create     | { form }                                                    | 新活动                                   |
| join       | id                                                          | 更新后活动；已关闭返回 ACTIVITY_CLOSED   |
| quit       | id                                                          | 更新后活动                               |
| toggle     | id                                                          | 更新后活动；非发起人返回 FORBIDDEN       |
| mine       | kind: published / joined                                    | 活动数组                                 |
| user       | —                                                          | 当前用户（兼容补 userId）                |
| login      | { phoneCode, profile }                                      | 用户信息                                 |
| updateUser | { userInfo }                                                | 用户信息                                 |
| qrcode     | id                                                          | { fileID }；懒生成活动详情小程序码并缓存 |
| feedback   | { feedback }                                                | { id, createTime }                       |

排序规则：hot → joinedCount 降序、latest → createTime 降序（广场默认 latest）、time → startTime 升序（接口保留，广场已不再暴露该筛选项）；广场的已关闭活动沉底，排在未关闭活动之后；我的参与按 startTime 升序、我的发布按 createTime 降序。
时间筛选：`weekday` 为 0~6 的星期几；`date` 为客户端按本地时区算好的当天 00:00 时间戳，服务端换算成 `[当天 00:00, 次日 00:00)` 区间匹配 `startTime`，两者由客户端保证互斥（不会同时下发）。

### 8.7 数据权限建议

- activities：通过云函数统一读写；“仅创建者可读写”或自定义安全规则；
- users：“仅创建者可读写”；
- banners：“仅管理员可读写”（云函数不受客户端权限限制）；
- feedback：“仅管理员可读写”。

---

## 9. 字典数据

### 9.1 活动类型字典


| key      | 名称   | emoji | 主题色  | 渐变               |
| -------- | ------ | ----- | ------- | ------------------ |
| hiking   | 徒步   | 🌿    | #4ECDC4 | #84fab0 → #8fd3f4 |
| climbing | 爬山   | 🥾    | #43cea2 | #43cea2 → #185a9d |
| driving  | 自驾游 | 🚗    | #45B7D1 | #89F7FE → #66A6FF |
| camping  | 露营   | ⛺    | #F6D365 | #f6d365 → #fda085 |
| cycling  | 骑行   | 🚴    | #44A08D | #a8edea → #fed6e3 |
| ball     | 打球   | 🏀    | #FF8E72 | #FFB88C → #FF8E72 |
| fitness  | 健身   | 💪    | #FA709A | #ffecd2 → #fcb69f |
| running  | 跑步   | 🏃    | #FFD93D | #ffecd2 → #fcb69f |
| swimming | 游泳   | 🏊    | #00CDAC | #74ebd5 → #9face6 |
| other    | 其他   | 🎉    | #A8DADC | #E0EAFC → #CFDEF3 |

### 9.2 标签与难度

- 活动标签：needOwner=缺车主、needPassenger=缺乘客；
- 难度：1星–10星。

### 9.3 城市数据

- 覆盖全国 34 个省级行政区与全部地级行政区（地级市 / 地区 / 自治州 / 盟），直辖市、特别行政区列本级；
- 不隶属任何地级市的省直辖县级行政区（如河南济源、湖北仙桃、新疆石河子）直接挂在所属省份下；
- 县级热门目的地（阳朔、义乌、香格里拉等）不进选择器，只在地址文本匹配时归到所属地级行政区；
- 首项为“全部”，用于不限城市浏览；
- 附带各主要城市经纬度表，用于无逆地址解析时的就近城市匹配；
- 城市匹配按“地址文本中出现省份名则优先在省内匹配 → 省内匹配不到再全局最长城市名匹配 → 最后看县级别名”策略，
  规避“朝阳 / 海东”等短名误匹配，也保证“青海省海南藏族自治州”这类同名省简称不会被海南省吃掉。

---

## 10. 非功能需求

### 10.1 性能

- 首页接口加载应有 Loading，列表接口目标首屏 < 1s（Mock 延时约 300ms 模拟）；
- 列表采用分页（广场 10 条 / 页），避免一次全量加载；
- 图片统一压缩上传，封面使用等比裁剪展示；列表图片预解析后展示。

### 10.2 兼容与发布

- 微信基础库 3.13.0+；
- 隐私接口：需声明定位与选点接口，并配置定位用途说明；
- 云开发需开通云环境，配置环境 ID，部署云函数，创建 4 个集合并设置权限；
- 主包体积控制：当前约 0.6M，复刻时组件按需引用，避免引入整包 UI 库导致超 2M 上限。

### 10.3 安全与合规

- 服务端一切数据操作经云函数，用户 openid 取自云函数上下文，不信任前端传入身份；
- 关闭 / 打开操作必须校验发起人 openid；
- 报名成员中的 openid 不返回给前端渲染（详情页只下发昵称头像）；
- 发布与报名前必须展示免责协议并经用户主动勾选；上线前需将协议主体替换为真实公司主体并完成法律审核；
- 内容层面：活动介绍、标题、反馈等文本需具备审核能力（当前为机器初审为主 + 人工审核台兜底：发布与编辑先进待审队列，`msgSecCheck` 同步检测文本、`mediaCheckAsync` 异步检测封面与群二维码、`img.scanQRCode` 识别群二维码内容且只认微信群邀请链接，机审全部通过时自动放行，疑似内容标记后交人工复核，违规内容直接拦下或自动下架，识别 / 检测接口异常时降级为纯人工审核；检测「没拿到结论」与「确实通过」在审核台分开显示，绝不把没结论当成通过 —— 图片推送没带结论时记 `failed` 转人工，文本在发布当刻没拿到结论时由图片回调补检一次，补到结论仍可自动放行）；
- 活动封面与群二维码是发起人上传的 `cloud://` 云存储文件，客户端能否直接渲染取决于云存储权限设置，而浏览者 / 审核人通常不是上传者：因此首页、广场、我的活动、活动详情的列表接口由 `activity` 云函数用管理员身份批量换 `https` 临时链接（`coverUrl` / `qrUrl`）再下发，审核台由 `admin` 云函数做同样处理，临时链接过期或加载失败时前端按 fileID 调 `media` 接口重取，服务端也取不到时如实提示「读不到 + fileID」；发布 / 编辑写库前同样校验一次，文件不存在直接要求重传，避免落库成永远加载不出的封面；
- 需配置《小程序用户隐私保护指引》，覆盖头像昵称、手机号、位置、选择的位置、选中的照片或视频、相册（仅写入）、设备信息、剪切板（仅写入）、发布内容等收集说明；小程序内《隐私政策》与个人中心的「注销账号」自助入口要和指引口径一致。

### 10.4 可维护性

- Mock 与云模式通过统一开关切换（useMock / useCloud），服务层接口签名一致；
- 协议文案集中管理，一处修改全局生效；
- 类型、城市、标签等字典集中配置；
- 横幅默认文案在云函数与 Mock 模型两侧保持同步（复刻时建议抽成单一默认数据源）。

---

## 11. 建议开发里程碑


| 阶段        | 内容                                                | 验收口径                             |
| ----------- | --------------------------------------------------- | ------------------------------------ |
| M1 骨架     | 工程初始化、UI 组件集成、自定义 Tab、路由、全局主题 | 三 Tab 可切换，页面标题正确          |
| M2 浏览链路 | 首页、广场、详情、活动卡片、城市定位与筛选          | Mock 数据可浏览全部推荐与列表        |
| M3 用户链路 | 发布、报名、退出、我的活动、个人中心、反馈          | 发布 → 报名 → 查看 → 退出闭环可用 |
| M4 分享增长 | 好友 / 朋友圈分享、海报生成保存                     | 海报可长按转发、保存成功             |
| M5 云开发   | 集合、云函数、图片上传、小程序码                    | 关闭 Mock 后全流程真实数据可用       |
| M6 上线准备 | 协议主体、隐私指引、内容审核、体验优化              | 通过微信审核并上线                   |

---

## 12. 附录

### 12.1 默认文案

- 应用名：旷行吖；
- 口号：和志同道合的人一起出发；
- 搜索占位：搜活动、地点，加入旷行吖 / 搜索活动、地点；
- 页脚：— 和志同道合的人一起出发 —；
- 版本：旷行吖 v1.0.0；
- 关于：旷行吖是一款户外运动组队小程序，支持自驾游、徒步、打球、骑行、健身、游泳、露营等玩法，一键发起活动、快速摇人组队。

### 12.2 默认横幅（3 条，可后台改）


| # | 标题             | 副标题                        | emoji | 动作                        |
| - | ---------------- | ----------------------------- | ----- | --------------------------- |
| 1 | 周末去山里走走吧 | 徒步 / 爬山 / 露营 一起出发   | 🏕️  | 广场，预选徒步              |
| 2 | 组局打球不孤单   | 篮球 / 足球 / 羽毛球 在线约战 | 🏀    | 广场，预选打球              |
| 3 | 想去看海呀       | 自驾拼车 说走就走             | 🌊    | 广场，预选自驾游（driving） |

三条默认横幅的 `action.type` 均为 `square`，点击后统一跳广场并用“当前定位城市 + 对应活动类型”筛选（例：定位成都时点第 3 条 → 广场筛选“成都 + 自驾游”）。

### 12.3 协议文案

发布协议：`《活动发布免责协议》`；报名协议：`《活动风险告知与免责协议》`。

两份协议均为多段结构化长文本，含“平台仅提供信息展示技术服务、不组织线下活动、责任由主办方与参与者自行承担、用户自愿参与、自甘风险、紧急救助授权、免责例外”等条款；协议中加粗标记的段落与子句在弹窗中以高亮粗体展示。当前源码中协议主体为“旷行吖科技（成都）有限公司”，正文与根目录同名 .docx 逐段一致；管辖法院等待定信息仍需法务确认后替换。

> 注：协议为法律文本且随主体与地区不同而调整，建议复刻时以源码协议配置文件为基准全文迁移，再由法务审阅替换主体、管辖法院等占位信息。

### 12.4 已知限制（复刻时按原样保留或评估升级）


| 项                       | 现状                                 |
| ------------------------ | ------------------------------------ |
| Mock 模式封面 / 群二维码 | 临时路径仅当前会话有效               |
| 发布可选历史日期         | 未限制不得早于今天                   |
| 时间精度                 | 发布时仅选择日期，不选具体时刻       |
| 城市归属                 | 依赖地点文本匹配，个别命名可能误判   |
| 活动管理                 | 发起人只能关闭 / 打开，无编辑、删除  |
| 内容安全                 | 未接微信内容安全接口，建议上线前补充 |
